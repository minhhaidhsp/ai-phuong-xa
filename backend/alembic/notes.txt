===========PG config
username: postgres
pass: postgres123

===========Run BE
cd D:\Projects\ai-phuong-xa\backend
.\venv\Scripts\Activate.ps1
uvicorn src.main:app --reload --host 0.0.0.0 --port 8000

Nhớ quy tắc: mỗi lần mở terminal mới đều phải cd backend + activate venv trước khi chạy bất kỳ lệnh nào!

========== Activate venv
.\venv\Scripts\Activate.ps1


======git config

git remote add origin https://github.com/minhhaidhsp/ai-phuong-xa.git
git config --global user.email "minhhaidhsp@gmail.com"
git config --global user.name "NM Hai"

=====push code
git add .
git commit -m "feat: Sprint 2 — JWT auth, RBAC, seed users"
git push -u origin main

====run seed
python seed.py

====Rub  qdrant
cd D:\qdrant
.\qdrant.exe

Qdrant sẽ chạy tại http://localhost:6333. 
Mở browser vào http://localhost:6333/dashboard để kiểm tra.